---
title: "My first quarto document"
format: html
editor: visual
---

# Big heading

## Quarto

Quarto enables you to weave together content and executable code into a finished document. To learn more about Quarto see <https://quarto.org>.

This is another paragraph. This is **bold** and this is *italics*. **also bold** and this is *italics*.

- This is the first item of an unordered list.
- Another item.

Then some more text

[This is linked text](https://quarto.org)

### Third level heading

## Running Code

When you click the **Render** button a document will be generated that includes both content and the output of embedded code. You can embed code like this:

```{r}
# this is a comment
1 + 1 * 46
```

You can add options to executable code like this

```{r}
#| echo: false
2 * 2
```

The `echo: false` option disables the printing of code (only output is displayed).

## Demonstration

```{r}
library(tidyverse)
library(here)
#read data with here
interviews_plotting <-read_csv(
  here("data", "interviews-plotting.csv"
  ))

interviews_plotting
```
```{r}
interviews_plotting |> 
  ggplot(aes(y = fct_infreq(respondent_wall_type))) + 
  geom_bar(aes(fill = village)) +
  labs(y = "Wall Type",
       x = NULL,
       fill = "Village") +
  theme_classic()
```

